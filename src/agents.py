from __future__ import annotations

"""
This module is intentionally thin for v1.

The agent behavior lives in src/service.py so the same logic can drive the
Streamlit demo and the FastAPI webhook endpoints.
"""

